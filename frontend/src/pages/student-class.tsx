import StudentClassTab from "@/components/student/class-tab";

/** Route wrapper for /class — student class overview + today's payment status */
export default function StudentClassPage() {
  return <StudentClassTab />;
}
