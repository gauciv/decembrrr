import StudentCalendarTab from "@/components/student/calendar-tab";

/** Route wrapper for /calendar — student read-only class calendar heatmap */
export default function StudentCalendarPage() {
  return <StudentCalendarTab />;
}
